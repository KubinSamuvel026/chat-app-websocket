{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat application with React frontend and Django backend",
  "private": true,
  "scripts": {
    "dev": "cd my-app && npm run dev",
    "build": "cd my-app && npm run build",
    "lint": "cd my-app && npm run lint",
    "preview": "cd my-app && npm run preview"
  },
  "devDependencies": {
    "rollup": "^4.54.0"
  }
}
